<module>
    <!-- module's name should match its executable file's name. -->
    <name>objectFinder</name>
    <description>Return the coordinates of the object, before and after the action</description>
    <version>0.1</version>

    <!-- <arguments> can have multiple <param> tags-->
    <arguments>
    <!-- <param default = "" desc="indices of desired features"> featInd </param> -->
 
        <param desc="Name of the module" default="objectFinder"> name</param>
        <param desc="Configuration file" default="objectFinder.ini"> from </param>

    </arguments>

    <!-- <authors> can have multiple <author> tags. -->
    <authors>
	  <author email=""> Tanis </author>
    </authors>

     <!-- <data> can have multiple <input> or <output> tags. -->
     <data>
        <input port_type="service">
            <type>rpc</type>
            <port carrier="tcp">/objectFinder/rpc:i</port>
            <description> Takes RPC commands from Human or stateMachine</description>
        </input>
        <input>
            <type>ImageOfPixelRgb</type>
            <port carrier="udp">/objectFinder/img:i</port>
            <required>no</required>
            <priority>no</priority>
            <description> Receives the camera image.</description>
        </input>
        
        <input>
            <type>Bottle</type>
            <port carrier="udp">/objectFinder/coords:i</port>
            <required>no</required>
            <priority>no</priority>
            <description> receives coords from yarpview out</description>
        </input>
        <input>
            <type>Bottle</type>
            <port carrier="udp">/objectFinder/track:i</port>
            <required>no</required>
            <priority>no</priority>
            <description> Receives coordinates from the tracker</description>
        </input>
        
        
        <!-- output data if available -->
        <output>
            <type>Bottle</type>
            <port carrier="udp">/objectFinder/coords:o</port>
            <description>Sends out the coords of the object</description>
        </output> 
        <output>
            <type>ImageOfPixelRgb</type>
            <port carrier="udp">/objectFinder/crop:o</port>
            <description>Sends out the desired image crop to initialize a tracker</description>
        </output> 
        <output>
            <type>ImageOfPixelRgb</type>
            <port carrier="udp">/objectFinder/imgOut:o</port>
            <description>Sends out images on the frames when the objects location is computed</description>
        </output> 
    </data>

    <dependencies>
        <computer>
        </computer>
    </dependencies>

    <!-- specific libraries or header files which are used for development -->
    <development>
        <header></header>
        <library>YARP</library>
        <library>OpenCV</library>
    </development>

</module>

