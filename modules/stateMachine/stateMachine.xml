<module>
    <!-- module's name should match its executable file's name. -->
    <name>stateMachine</name>
    <description> Coordinates the modules used for Affordance Learning</description>
    <version>0.5</version>

    <!-- <arguments> can have multiple <param> tags-->
    <arguments>
    <!--
    <param desc="Name of the module" default="nearThingsDetector"> name</param>
    <param desc="Sub-path from \c $ICUB_ROOT/app to the configuration file" default="nearThingsDetector/conf"> context </param>
	<param desc="Configuration file" default="config.ini"> from </param>
    -->
    </arguments>

    <!-- <authors> can have multiple <author> tags. -->
    <authors>
	  <author email=""> Tanis Mar</author>
    </authors>

     <!-- <data> can have multiple <input> or <output> tags. -->
     <data>
        <!-- input data if available -->
        <input type="service">
            <type>rpc</type>
            <port carrier="tcp">/affManager/rpc:i</port>
            <description>takes RPC commands from Human or stateMachine</description>
        </input>
        
        <!-- output data if available -->
        <output type="service">
            <type>rpc</type>
            <port carrier="udp">/stateMachine/rpc:o</port>
            <description> Send commands to the modules involved in the affordance Experiment</description>
        </output>
    </data>

    <dependencies>
        <computer>
        </computer>
    </dependencies>

    <!-- specific libraries or header files which are used for development -->
    <development>
        <header></header>
        <library>YARP</library>
    </development>

</module>

